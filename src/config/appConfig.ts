/**
 * Application configuration
 * Update these values with each new release
 */
export const appConfig = {
  // Current version number (update with each release)
  version: "0.1.2",

  // Date of last update (update with each release)
  lastUpdated: "2024-04-23",

  // App name
  appName: "CompGen - React Component Generator",
};
